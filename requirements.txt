# ─── PerimeterExplorer — Python dependencies ──────────────────────────────────
# Install with:  pip install -r requirements.txt
#
# ── External tools (NOT pip packages — install separately) ────────────────────
#   subfinder   → go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
#   assetfinder → go install github.com/tomnomnom/assetfinder@latest
#   findomain   → https://github.com/Findomain/Findomain/releases
#   amass       → go install -v github.com/owasp-amass/amass/v4/...@master
#   dnsrecon    → apt install dnsrecon   OR   git clone https://github.com/darkoperator/dnsrecon
#   fierce      → apt install fierce     OR   git clone https://github.com/mschwager/fierce
#
# ──────────────────────────────────────────────────────────────────────────────

# HTTP client — used for crt.sh, Wayback Machine, VirusTotal API calls
requests>=2.31.0

# Cross-platform coloured terminal output
colorama>=0.4.6

# DNS library (used by dnsrecon and useful for manual subdomain validation)
dnspython>=2.4.0
